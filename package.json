{
  "name": "tokenizer",
  "version": "3.10.1",
  "license": "MIT",
  "scripts": {
    "lint": "npx eslint src/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "eslint": "^8.11.0",
    "jest": "^29.0.3",
    "prettier": "2.0.5"
  }
}

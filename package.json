{
  "name": "tokenizer",
  "version": "3.7.1",
  "license": "MIT",
  "scripts": {
    "lint": "npx eslint src/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "eslint": "^8.11.0",
    "jest": "^25.1.0",
    "prettier": "2.0.5"
  }
}
